# Protocol: incomplete-protocol

This protocol directory intentionally omits schema.json.
Used to exercise the hasSchema=false branch in protocol loader tests.
A protocol without a schema cannot be used as a validator target.
