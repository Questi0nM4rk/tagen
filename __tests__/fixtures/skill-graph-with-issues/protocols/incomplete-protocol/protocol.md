# Protocol: incomplete-protocol

This directory is intentionally incomplete — missing schema.json, validator.ts,
and examples/ — used to drive the negative branches of `loadProtocols` and
`validate`. Do not promote it to the canonical fixture.
